<UserControl x:Class="PCClubAdmin.Views.ClientsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Управление клиентами" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

        <!-- Панель управления -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Добавить клиента" Command="{Binding ShowAddDialogCommand}" Width="120" Height="30" Margin="0,0,10,0"/>
            <Button Content="Обновить список" Command="{Binding RefreshClientsCommand}" Width="120" Height="30"/>
        </StackPanel>

        <!-- Диалоговое окно добавления клиента -->
        <Border Grid.Row="2" Visibility="{Binding IsAddDialogVisible, Converter={StaticResource BooleanToVisibilityConverter}}" Background="White" BorderBrush="Gray" BorderThickness="1" Padding="20" CornerRadius="5" Margin="0,0,0,10">
            <StackPanel>
                <TextBlock Text="{Binding DialogTitle}" FontSize="16" FontWeight="Bold" Margin="0,0,0,15"/>

                <StackPanel Margin="0,0,0,10">
                    <TextBlock Text="Имя:" FontWeight="SemiBold"/>
                    <TextBox Height="25" Margin="0,5,0,0" Text="{Binding ClientName, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>

                <StackPanel Margin="0,0,0,10">
                    <TextBlock Text="Телефон:" FontWeight="SemiBold"/>
                    <TextBox Height="25" Margin="0,5,0,0" Text="{Binding ClientPhone, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>

                <StackPanel Margin="0,0,0,10">
                    <TextBlock Text="Email:" FontWeight="SemiBold"/>
                    <TextBox Height="25" Margin="0,5,0,0" Text="{Binding ClientEmail, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>

                <StackPanel Margin="0,0,0,15">
                    <TextBlock Text="Начальный баланс (руб):" FontWeight="SemiBold"/>
                    <TextBox Height="25" Margin="0,5,0,0" Text="{Binding ClientBalance, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="{Binding SaveButtonText}" Command="{Binding SaveClientCommand}" Width="80" Margin="0,0,10,0"/>
                    <Button Content="Отмена" Command="{Binding CancelAddClientCommand}" Width="80"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Таблица клиентов -->
        <DataGrid Grid.Row="3" AutoGenerateColumns="False" CanUserAddRows="False" ItemsSource="{Binding Clients}" HorizontalScrollBarVisibility="Disabled">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="Auto" MinWidth="38"/>
                <DataGridTextColumn Header="Имя" Binding="{Binding Name}" Width="1.3*" MinWidth="110"/>
                <DataGridTextColumn Header="Телефон" Binding="{Binding Phone}" Width="1.2*" MinWidth="112"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="1.4*" MinWidth="135"/>
                <DataGridTextColumn Header="Баланс" Binding="{Binding Balance, StringFormat={}{0:C}}" Width="Auto" MinWidth="80"/>
                <DataGridTemplateColumn Header="Действия" Width="Auto" MinWidth="165">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Редактировать"
                                        Command="{Binding DataContext.EditClientCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"
                                        Margin="0,0,5,0"/>
                                <Button Content="Удалить"
                                        Command="{Binding DataContext.DeleteClientCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
