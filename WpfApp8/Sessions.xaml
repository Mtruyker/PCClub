<UserControl x:Class="PCClubAdmin.Views.SessionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:infrastructure="clr-namespace:PCClubAdmin.Infrastructure">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <infrastructure:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
    </UserControl.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Управление сессиями" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

        <!-- Панель управления -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Добавить сессию" Command="{Binding ShowAddDialogCommand}" Width="120" Height="30" Margin="0,0,10,0"/>
            <Button Content="Обновить список" Command="{Binding RefreshSessionsCommand}" Width="120" Height="30"/>
        </StackPanel>

        <!-- Диалоговое окно добавления сессии -->
        <Border Grid.Row="2" Visibility="{Binding IsAddDialogVisible, Converter={StaticResource BooleanToVisibilityConverter}}" Background="White" BorderBrush="Gray" BorderThickness="1" Padding="20" CornerRadius="5" Margin="0,0,0,10">
            <StackPanel>
                <TextBlock Text="Добавление новой сессии" FontSize="16" FontWeight="Bold" Margin="0,0,0,15"/>

                <StackPanel Margin="0,0,0,10">
                    <TextBlock Text="Клиент:" FontWeight="SemiBold"/>
                    <ComboBox Height="25" Margin="0,5,0,0"
                              ItemsSource="{Binding AvailableClients}"
                              SelectedItem="{Binding SelectedClientName}"/>
                </StackPanel>

                <StackPanel Margin="0,0,0,10">
                    <TextBlock Text="Компьютер:" FontWeight="SemiBold"/>
                    <ComboBox Height="25" Margin="0,5,0,0"
                              ItemsSource="{Binding AvailableComputers}"
                              SelectedItem="{Binding SelectedComputerName}"/>
                </StackPanel>

                <StackPanel Margin="0,0,0,10">
                    <TextBlock Text="Тариф:" FontWeight="SemiBold"/>
                    <ComboBox Height="25" Margin="0,5,0,0"
                              ItemsSource="{Binding AvailableTariffs}"
                              SelectedItem="{Binding SelectedTariff}"
                              DisplayMemberPath="Name"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Сохранить" Command="{Binding SaveSessionCommand}" Width="80" Margin="0,0,10,0"/>
                    <Button Content="Отмена" Command="{Binding CancelAddSessionCommand}" Width="80"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Таблица сессий -->
        <DataGrid Grid.Row="3" AutoGenerateColumns="False" CanUserAddRows="False" ItemsSource="{Binding Sessions}" HorizontalScrollBarVisibility="Auto">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="Auto" MinWidth="45"/>
                <DataGridTextColumn Header="Клиент" Binding="{Binding ClientName}" Width="1.4*" MinWidth="140"/>
                <DataGridTextColumn Header="Компьютер" Binding="{Binding ComputerName}" Width="Auto" MinWidth="105"/>
                <DataGridTextColumn Header="Тариф" Binding="{Binding TariffName}" Width="*" MinWidth="100"/>
                <DataGridTextColumn Header="Начало" Binding="{Binding FormattedStartTime}" Width="Auto" MinWidth="130"/>
                <DataGridTextColumn Header="Окончание" Binding="{Binding FormattedEndTime}" Width="Auto" MinWidth="130"/>
                <DataGridTextColumn Header="Продолжительность" Binding="{Binding Duration}" Width="Auto" MinWidth="120"/>
                <DataGridTextColumn Header="Стоимость" Binding="{Binding FormattedCost}" Width="Auto" MinWidth="100"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="Auto" MinWidth="85"/>
                <DataGridTemplateColumn Header="Действия" Width="Auto" MinWidth="170">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Завершить"
                                        Command="{Binding DataContext.EndSessionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"
                                        Visibility="{Binding EndTime, Converter={StaticResource NullToVisibilityConverter}}"
                                        Margin="0,0,5,0"/>
                                <Button Content="Удалить"
                                        Command="{Binding DataContext.DeleteSessionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
